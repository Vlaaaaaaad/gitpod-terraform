#!/bin/bash

# Entyrpoint for Gitpod workspace setup

while :; do
  echo " "
  echo " "
  echo " "
  echo "Hello Gitpod user!"
  echo " "
  echo "This script will help setup Gitpod"
  echo " "
  echo "There are 2 components:"
  echo "1. SSH key setup"
  echo "2. GPG key setup"
  echo " "
  read -p -r "Press enter to continue or Control-C( ^C ) to quit without any setup"

  echo " TODO: This is still to be implemented"
  # read -s -n 1 answer
  # if [ $answer = "y" || $answer = "Y" ]; then echo "todo"; break
  # elif [ $answer = "n" || $answer = "N" ]; then echo "also todo"; break
  # elif [ $answer = "" ]; then continue
  # fi
done
